name: Validate protocol JSON

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate protocol JSON
        run: python -m json.tool protocol/protocol.json
